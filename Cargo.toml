[workspace]
resolver = "2"
members = [
    "barcode-proto",
    "receiver",
]

# android/rust is built separately via cargo-ndk (see .github/workflows/release.yml)
# ios/rust is built separately by build-ios.sh (requires macOS + iOS toolchain)
exclude = ["android/rust", "ios/rust"]
